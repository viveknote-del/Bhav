# {{PROJECT_DISPLAY_NAME}} — Bug Tracker

Bug format written by `/bug` command. Priority: P0 (critical) → P3 (minor).

P0 bugs trigger immediate hotfix branches and bypass the normal pipeline.

---

*(No bugs logged yet. Use `/bug "description"` to log one.)*
