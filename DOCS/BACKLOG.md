# {{PROJECT_DISPLAY_NAME}} — Feature Backlog

Feature requests from the user. Each is logged by `/feature` and assigned a future step.

---

*(No features logged yet. Use `/feature "idea"` to add one.)*
